Networks
========

The ``network`` module replaces the ``brian2.Network`` objects, while adding a
management of monitors and extra functionality to runs.

.. toctree::
    :caption: Available networks

    IONetwork
